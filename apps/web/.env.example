API_URL=http://localhost:3001

# WebSocket endpoint for Insider Chat (defaults to page origin in production).
NEXT_PUBLIC_WS_URL=http://localhost:3001

# Browser uses /api/proxy — do not expose API tokens client-side.
# S3 uploads are handled server-side via presigned URLs from the API.